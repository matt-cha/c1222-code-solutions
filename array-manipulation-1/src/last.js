/* exported last */
/* -create a function that returns the last element on an array
argument passed.
-return the last element of the array at index of the length of the array
subtracted by 1 */
function last(array) {
  return array[array.length - 1];
}
