/* exported invert */
/* -create a function that takes 1 argument
-create a new object
-create a for loop that stops at the source length
-create a variable that sets  */
/* function invert(source) {
  var newObject = {};
  var newKeys = [];
  var newValues = [];
  for (var key in source) {
    newKeys = source[key];
  }
  return newKeys;
} */
