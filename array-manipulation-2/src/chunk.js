/* exported chunk */
/* -create a function with 2 arguments
-create a empty array that will contain the array of arrays
-need to create one main array that returns  */
/* function chunk(array, size) {
  var newArray = array.split('', 0, size);
  console.log(newArray);
  for (var i = 0; i < size; i++) {
    newArray.push(array[i]);
  }
  return newArray.join('');
} */
/* need to use slice? */
/* function chunk(array, size) {
  var firstArray = array.slice(0, size);
  var secondArray = array.slice(size, array.length - 1);
  var finalArray =
  console.log('firstArray:', firstArray);

  return firstArray;
}
 */
