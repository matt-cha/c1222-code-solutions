/* exported difference */
/* -create a function that has 2 arguments
-create a nested for loop that stops at either argument
-check if the index at each of the arguments is equal
-if not, push to a new array */
/* function difference(first, second) {
  var differenceArray = [];
  for (var i = 0; i < first.length; i++) {
    for (var k = 0; k < second.length; k++) {
      console.log(first[i]);
      if (first[i] !== second[k]) {
        differenceArray.push(first[i], second[k]);
      }
    }
  }
  return differenceArray;
}
 */
