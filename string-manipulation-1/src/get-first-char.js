/* exported getFirstChar */
/* create a function that returns the first character
of the string argument passed to the function by returning the 0 index
of the string. */
function getFirstChar(string) {
  return string[0];
}
